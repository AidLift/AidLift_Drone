{
  "name": "aidlift_drone",
  "version": "1.0.0",
  "description": "Project Overview\r AidLift is an innovative AI-powered autonomous drone system designed to revolutionize emergency response and humanitarian aid during natural disasters.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npm run start",
    "clean": "npm install rimraf && rimraf node_modules client/node_modules server/node_modules client/dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
